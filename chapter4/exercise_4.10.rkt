;2024-08-03

; I feel like this one is pretty open ended. Since we aren't allowed to change eval or apply, we would be limited to updating the fuctions that are provided in each of the different types of operations that the eval can apply one

; Since the syntax depends on how you identify each different data type or procedure, there really isn't a limit to how you can modify the syntax of the new language as long as you give eval and apply the right procedures to interpret the data structure in an appropriate way.

; As for actually doing so, I don't feel like this question really provides a challenging problem to solve since it is so open ended and it isn't really going to be an issue trying to change the syntax at all, so I will just leave it at that.
