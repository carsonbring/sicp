(import (chibi))
(define (power x y) 
  (if (= y 0)
    1
    (* x (power x (- y 1)))
  )
)
(define (cons a b)
   (* (power 2 a) (power 3 b))
)
(define (elim-base a b)
  (if (> (modulo a b) 0)
    a
    (elim-base (/ a b) b )
  )
)
(define (car z) 
  (logarithm (elim-base z 3) 2)
)
(define (cdr z)
  (logarithm (elim-base z 2) 3)
)
(define (logarithm a b)
  (/ (log a) (log b))
)


(display (car (cons 5 3)))
(newline)
(display (cdr (cons 5 3)))
